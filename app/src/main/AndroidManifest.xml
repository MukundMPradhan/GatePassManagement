<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.app.myandroid.gatepassmngtsystem">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".entry.UserSignUpActivity"
            android:windowSoftInputMode="stateHidden" />


        <activity android:name=".WelcomeActivity" />


        <activity
            android:name=".entry.New_Individual_Pass"
            android:windowSoftInputMode="stateHidden">

        </activity>

        <activity
            android:name=".entry.New_Vehicle_Pass"
            android:windowSoftInputMode="stateHidden" />


        <activity
            android:name=".entry.New_Company_Add"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity android:name=".entry.LogInScreen"></activity>
    </application>

</manifest>